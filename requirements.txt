requests
beautifulsoup4
lxml # often a dependency for bs4, good to include explicitly
python-dotenv # If environment variables are used (e.g., for API keys)
Flask # If a web interface is planned (e.g. for Streamlit)
pandas # Useful for data manipulation, although not strictly required by core ETL
numpy # Often a dependency for pandas
SQLAlchemy # For more advanced ORM features, if needed for future expansion
apache-airflow # Not used in this version but a common dependency if Airflow was considered
